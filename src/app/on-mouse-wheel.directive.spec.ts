import { OnMouseWheelDirective } from './on-mouse-wheel.directive';

describe('OnMouseWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new OnMouseWheelDirective();
    expect(directive).toBeTruthy();
  });
});
