import { RedactDirective } from './redact.directive';

describe('RedactDirective', () => {
  it('should create an instance', () => {
    const directive = new RedactDirective();
    expect(directive).toBeTruthy();
  });
});
